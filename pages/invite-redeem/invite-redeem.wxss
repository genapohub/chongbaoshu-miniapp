/* 邀请码兑换页 - 样式 */
.redeem-page {
  min-height: 100vh;
  background: #f5f5f5;
}

/* 结果状态（成功/错误） */
.result-section {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 120rpx 60rpx;
  background: #fff;
  min-height: 100vh;
}

.result-icon {
  font-size: 160rpx;
  margin-bottom: 40rpx;
}

.result-icon.success {
  color: #4CAF50;
}

.result-icon.error {
  font-size: 120rpx;
  color: #4A8C5C;
  width: 160rpx;
  height: 160rpx;
  line-height: 160rpx;
  text-align: center;
  border-radius: 50%;
  background: rgba(51, 112, 255, 0.1);
}

.result-title {
  font-size: 40rpx;
  font-weight: 700;
  color: #2D2A26;
  margin-bottom: 20rpx;
}

.result-desc {
  font-size: 28rpx;
  color: #5E5953;
  margin-bottom: 80rpx;
  text-align: center;
}

.btn-action {
  width: 360rpx;
  height: 88rpx;
  line-height: 88rpx;
  background: #4A8C5C;
  color: #fff;
  border-radius: 44rpx;
  font-size: 32rpx;
  font-weight: 500;
  border: none;
}

/* 表单区 */
.form-section {
  padding: 60rpx 40rpx;
}

.form-header {
  text-align: center;
  margin-bottom: 60rpx;
  background: linear-gradient(135deg, #4A8C5C, #245BDB);
  margin: -40rpx -40rpx 60rpx;
  padding: 80rpx 40rpx 60rpx;
  border-radius: 0 0 40rpx 40rpx;
  color: #fff;
}

.header-icon {
  font-size: 80rpx;
  margin-bottom: 20rpx;
}

.header-title {
  font-size: 36rpx;
  font-weight: 600;
  color: #fff;
  margin-bottom: 12rpx;
}

.header-desc {
  font-size: 26rpx;
  color: rgba(255, 255, 255, 0.9);
}

/* 输入框 */
.code-input-group {
  display: flex;
  align-items: center;
  background: #fff;
  border-radius: 16rpx;
  padding: 0 32rpx;
  height: 100rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.code-input {
  flex: 1;
  height: 100rpx;
  font-size: 40rpx;
  font-weight: 600;
  letter-spacing: 8rpx;
  color: #2D2A26;
  text-align: center;
}

.btn-paste {
  font-size: 28rpx;
  color: #4A8C5C;
  padding: 10rpx 24rpx;
}

/* 错误信息 */
.error-msg {
  font-size: 24rpx;
  color: #4A8C5C;
  margin-bottom: 20rpx;
  padding-left: 10rpx;
}

/* 兑换按钮 */
.btn-redeem {
  width: 100%;
  height: 96rpx;
  line-height: 96rpx;
  background: #4A8C5C;
  color: #fff;
  border-radius: 48rpx;
  font-size: 34rpx;
  font-weight: 600;
  border: none;
  margin-top: 20rpx;
}

.btn-redeem[disabled] {
  background: #ccc;
  color: #fff;
}

/* 使用说明 */
.tips {
  margin-top: 60rpx;
  padding: 30rpx;
  background: #fff;
  border-radius: 16rpx;
  box-shadow: 0 2rpx 10rpx rgba(0, 0, 0, 0.05);
}

.tip-title {
  font-size: 28rpx;
  font-weight: 600;
  color: #2D2A26;
  margin-bottom: 16rpx;
}

.tip-item {
  font-size: 24rpx;
  color: #5E5953;
  line-height: 2;
}