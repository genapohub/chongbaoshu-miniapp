<view class="container">
  <view class="header-bar"><text class="title">同窝管理</text></view>
  <view wx:if="{{loading}}" class="loading">加载中...</view>
  <view wx:elif="{{litters.length===0}}" class="empty"><icon name="baby" size="80" color="#C0C4CC"/><text class="empty-text">暂无同窝记录</text><text class="empty-desc">在繁育记录中标记"已分娩"时自动创建</text></view>
  <view wx:else class="list">
    <view wx:for="{{litters}}" wx:key="id" class="card" bindtap="goDetail" data-id="{{item.id}}">
      <view class="card-top">
        <text class="card-mother">母：{{item.mother_name||'?'}}</text>
        <text class="card-father" wx:if="{{item.father_name}}">父：{{item.father_name}}</text>
      </view>
      <view class="card-meta">
        <text>出生：{{item.birth_date}}</text>
        <text>幼犬：{{item.puppy_count||0}}/{{item.litter_size}}</text>
      </view>
      <view class="card-actions" wx:if="{{item.puppy_count < item.litter_size}}">
        <view class="btn-batch" catchtap="batchAdd" data-id="{{item.id}}">补齐幼犬</view>
      </view>
    </view>
  </view>
</view>
