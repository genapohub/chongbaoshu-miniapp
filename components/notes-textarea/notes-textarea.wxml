<view class="notes-textarea">
  <textarea
    class="notes-input"
    value="{{value}}"
    placeholder="{{placeholder}}"
    maxlength="{{maxlength}}"
    bindinput="onInput"
    auto-height
  />
  <view class="notes-counter">{{currentLength}}/{{maxlength}}</view>
</view>
